title: Edge Intrusion Detector
description: A centralized Python Base Station driving real-time anomaly detection via the deterministic MicroShield C library on resource-constrained Edge devices.
remote_theme: pmarsceill/just-the-docs
color_scheme: "light"
search_enabled: true
plugins:
  - jekyll-remote-theme
defaults:
  - scope:
      path: ""
    values:
      layout: default
